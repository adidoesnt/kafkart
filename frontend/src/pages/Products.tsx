import { Layout } from "@/components/Layout";

function Products() {
    return <Layout>
        <div className="flex w-full h-full justify-center items-center">Products</div>
    </Layout>
}

export default Products;
